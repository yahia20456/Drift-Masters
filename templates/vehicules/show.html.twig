{% extends 'base.html.twig' %}

{% block title %}Vehicule{% endblock %}

{% block body %}
    <h1>Vehicule</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Idvehicule</th>
                <td>{{ vehicule.idvehicule }}</td>
            </tr>
            <tr>
                <th>Marqueveh</th>
                <td>{{ vehicule.marqueveh }}</td>
            </tr>
            <tr>
                <th>Modeleveh</th>
                <td>{{ vehicule.modeleveh }}</td>
            </tr>
            <tr>
                <th>Couleurveh</th>
                <td>{{ vehicule.couleurveh }}</td>
            </tr>
            <tr>
                <th>Agevehicule</th>
                <td>{{ vehicule.agevehicule }}</td>
            </tr>
            <tr>
                <th>Puissanceveh</th>
                <td>{{ vehicule.puissanceveh }}</td>
            </tr>
            <tr>
                <th>Cylindre</th>
                <td>{{ vehicule.cylindre }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('vehicules_index') }}">back to list</a>

    <a href="{{ path('vehicules_edit', {'idvehicule': vehicule.idvehicule}) }}">edit</a>

    {{ include('vehicules/_delete_form.html.twig') }}
{% endblock %}
